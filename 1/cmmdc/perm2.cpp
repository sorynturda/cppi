#include <bits/stdc++.h>

using namespace std;

ifstream in("perm2.in");
ofstream out("perm2.out");

int main() {

    int n, lcm=1;
    cin >> n;
    vector<int>v(n + 1), viz(n + 1);
    for (int i = 1; i <= n; i++) {
        cin >> v[i];
    }
    for (int i = 1; i <= n; i++) {
        
    }
    return 0;
}